# API Configuration
# Note: These are automatically set based on environment:
# - Development: .env.development (local API server)
# - Production: .env.production (production API server) 
# - Override: .env (overrides environment-specific files)

# Production (set in Vercel environment variables)
VITE_API_BASE_URL=https://api.eigentask.co.uk/api/v1
VITE_OAUTH_BASE_URL=https://api.eigentask.co.uk

# Development (set in .env.development)  
# VITE_API_BASE_URL=http://127.0.0.1:8000/api/v1
# VITE_OAUTH_BASE_URL=http://127.0.0.1:8000